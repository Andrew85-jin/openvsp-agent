export default function WelcomePanel() {
  return (
    <section className="hero">
      <p className="eyebrow">Electron + React</p>
      <h1>OpenVSP Agent</h1>
      <p className="lead">
        React is now running inside the Electron renderer process.
      </p>
    </section>
  );
}
